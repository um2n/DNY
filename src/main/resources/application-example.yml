spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dny?serverTimezone=Asia/Seoul&rewriteBatchedStatements=true
    username: root
    password: 'mysql 비번'
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: false
        jdbc:
          batch_size: 100
        order_inserts: true
        order_updates: true

job:
  api:
    key: 'api키'
